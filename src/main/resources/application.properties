# Database configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/students
spring.datasource.username=root
spring.datasource.password=MySQL_08/09/02
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Use create or create-drop for testing and learning.
#Use update for production scenarios to retain data.
#Use validate to ensure the schema is correct without making changes.
#Use none when you want to manage the schema manually.
